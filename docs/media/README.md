# Демо-материалы

## `IMG_9299.MP4`

Запись работы VKEncrypt P2P в реальном чате ВКонтакте: видно, как обычный текст уходит в виде `ENC[k1:base64...]` и расшифровывается расширением на стороне получателя.

Файл — оригинальное видео с iPhone (master-запись экрана), без монтажа. Используется в корневом `README.md`.
